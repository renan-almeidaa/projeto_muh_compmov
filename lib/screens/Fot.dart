import 'package:flutter/material.dart';

class Fot extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.indigo,
      child: Center(
        child: Text(
            "texto"
        ),
      ),
    );
  }
}
